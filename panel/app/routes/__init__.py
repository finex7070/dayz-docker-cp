"""Blueprints, one module per section of the panel.

Sections whose functionality arrives in a later phase already have their
blueprint and page here, so navigation and URLs stay stable while the features
are filled in.
"""
